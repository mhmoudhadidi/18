if (input.temperature() > 0) {
    basic.showString("the temperature is above zero")
    if (input.temperature() > 0) {
        basic.showString("the temperature is below zero")
    } else {
        basic.showString("the temperature is zero")
    }
    
}

